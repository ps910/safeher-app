<?xml version="1.0" encoding="utf-8"?>
<!-- Security: Network security configuration (Vuln #11)
     - Prevents cleartext (HTTP) traffic in release builds
     - Restricts trusted CAs to system store only (no user-installed certs) -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext for localhost in debug builds -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
</network-security-config>
